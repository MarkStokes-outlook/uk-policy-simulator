# Local Exchange

Optional per-repo message bus.

Global, cross-project queues should live under the root DaemonCore `exchange/` directory.
This folder is for **repository-local** routing only, if the project needs it.

If unused, it can remain empty.
