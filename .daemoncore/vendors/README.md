# Local Vendor Profiles (Optional)

Most model/vendor configuration should live in the global DaemonCore `vendors/` directory.

Use this folder only if this repository needs repo-specific overrides or capabilities
for a given vendor or model (for example, experimental roles or project-locked capabilities).
