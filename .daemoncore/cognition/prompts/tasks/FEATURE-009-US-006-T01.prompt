# Task FEATURE-009-US-006-T01 — Implement: Secret-handling security tests

> **DaemonCore coding-agent prompt.** Work type: `feature` · Size: `M` · Priority: `P2` · Status: `todo`

## Where this fits
- **Epic EPIC-009 — Bring Your Own AI.** Allow users to bring their own AI credentials, configured per user, with encrypted storage and quota awareness - while guaranteeing the platform remains fully usable with no paid API via local Ollama.
- **Feature FEATURE-009 — Bring Your Own AI — implementation.**
- **Story FEATURE-009-US-006 — Secret-handling security tests.** _As a maintainer, I want tests proving keys never leak so BYO-AI is safe._

## Objective
Implement the tests for: Secret-handling security tests.

## What to do
Write **deterministic automated tests** that cover each acceptance criterion of the parent story:
- Assert: Tests assert no plaintext at rest
- Assert: No key in logs or responses
- Assert: Correct decrypt-in-memory behaviour
Use fixed, hand-computed numbers (see `tests/test_fiscal_model.py`). Avoid randomness, network calls and time-dependence.

## Acceptance criteria (Definition of Done)
- [ ] Tests assert no plaintext at rest
- [ ] No key in logs or responses
- [ ] Correct decrypt-in-memory behaviour
- [ ] New/updated tests pass and cover the above
- [ ] No regression in the existing `pytest` suite

## Dependencies
None within this story. (Respect cross-epic dependencies of the parent epic.)

## Project conventions
- Target Python 3.13. Use British spelling in all user-facing text.
- Keep the pure model layer (`model/`) free of Streamlit; `app.py` is UI/render only.
- Pin any new runtime deps in `requirements.txt`; test-only deps in `requirements-dev.txt`.
- Write deterministic `pytest` tests; follow the style of `tests/test_fiscal_model.py`.
- Match the surrounding code style. Prefer small, coherent diffs over broad refactors.
- Update `README.md` / `docs/` whenever behaviour changes.

## Guardrails (EPIC-009)
- Encrypt API keys at rest; never log or render them; decrypt only in-memory.
- The platform MUST work end-to-end with no keys via local Ollama.
- Provide add / rotate / remove key management.

## Verification
- `python3 -m py_compile <changed .py files>`
- `python3 -m pytest -q` — all tests pass (add/extend tests for this change).

## Notes
- This simulator is a transparent scenario sandbox, not an official forecast — keep that framing in any user-facing text.
- If requirements are ambiguous or you must change a public interface, stop and flag it rather than guessing.
