# Task FEATURE-012-US-001-T01 — Implement: Engine interface for pluggable models

> **DaemonCore coding-agent prompt.** Work type: `feature` · Size: `M` · Priority: `P3` · Status: `todo`

## Where this fits
- **Epic EPIC-012 — Advanced Socio-Economic Engine.** Replace the simple annual-feedback model with a richer multi-channel socio-economic engine (labour, productivity, education, housing, NHS, welfare, crime, long-term effects) supporting conservative/moderate/optimistic forecast modes and explicit uncertainty.
- **Feature FEATURE-012 — Advanced Socio-Economic Engine — implementation.**
- **Story FEATURE-012-US-001 — Engine interface for pluggable models.** _As an architect, I want the engine to accept swappable models so the simple one can be replaced safely._

## Objective
As an architect, I want the engine to accept swappable models so the simple one can be replaced safely.

## What to do
Implement the capability so that every acceptance criterion below holds. Specifically:
- A feedback-strategy interface exists
- The current model is one implementation
- Callers are unaffected by the swap
Add or update docstrings and any necessary types. Keep the change focused on this story; do not pull in scope from other stories/epics.

## Acceptance criteria (Definition of Done)
- [ ] A feedback-strategy interface exists
- [ ] The current model is one implementation
- [ ] Callers are unaffected by the swap
- [ ] New/updated tests pass and cover the above
- [ ] No regression in the existing `pytest` suite

## Dependencies
None within this story. (Respect cross-epic dependencies of the parent epic.)

## Project conventions
- Target Python 3.13. Use British spelling in all user-facing text.
- Keep the pure model layer (`model/`) free of Streamlit; `app.py` is UI/render only.
- Pin any new runtime deps in `requirements.txt`; test-only deps in `requirements-dev.txt`.
- Write deterministic `pytest` tests; follow the style of `tests/test_fiscal_model.py`.
- Match the surrounding code style. Prefer small, coherent diffs over broad refactors.
- Update `README.md` / `docs/` whenever behaviour changes.

## Guardrails (EPIC-012)
- Introduce the new engine behind the pluggable feedback-strategy interface (EPIC-001).
- Preserve determinism per forecast mode; represent uncertainty explicitly.
- Document every channel with sources; re-baseline regression tests deliberately.

## Verification
- `python3 -m py_compile <changed .py files>`
- `python3 -m pytest -q` — all tests pass (add/extend tests for this change).

## Notes
- This simulator is a transparent scenario sandbox, not an official forecast — keep that framing in any user-facing text.
- If requirements are ambiguous or you must change a public interface, stop and flag it rather than guessing.
